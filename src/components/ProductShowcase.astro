---
const ingredients = [
  { name: "DHM", description: "Liver support from Hovenia Dulcis", icon: "🌿" },
  { name: "Milk Thistle", description: "Traditional detox herb", icon: "🌸" },
  { name: "B-Vitamins", description: "Energy metabolism", icon: "⚡" },
  { name: "Electrolytes", description: "Deep hydration", icon: "💧" }
];
---

<section class="py-32 px-6 relative">
  
  <div class="max-w-5xl mx-auto">
    
    <!-- Header -->
    <div class="text-center mb-16">
      <p class="text-[#c8e321] text-sm font-medium uppercase tracking-wider mb-4">Formula</p>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Science-backed ingredients
      </h2>
      <p class="text-white/50 text-lg max-w-2xl mx-auto">
        Every ingredient is carefully selected to support your body's natural recovery.
      </p>
    </div>

    <!-- Ingredients Grid -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-16">
      {ingredients.map((ingredient) => (
        <div class="text-center group">
          <div class="text-5xl mb-4 group-hover:scale-110 transition-transform duration-300">{ingredient.icon}</div>
          <h4 class="text-white font-semibold mb-1">{ingredient.name}</h4>
          <p class="text-white/40 text-sm">{ingredient.description}</p>
        </div>
      ))}
    </div>

    <!-- Certifications -->
    <div class="flex flex-wrap items-center justify-center gap-8 text-white/40 text-sm">
      <span class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-[#c8e321]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Vegan
      </span>
      <span class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-[#c8e321]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Gluten-Free
      </span>
      <span class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-[#c8e321]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        No Artificial Sweeteners
      </span>
      <span class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-[#c8e321]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Made in EU
      </span>
    </div>

  </div>
</section>
